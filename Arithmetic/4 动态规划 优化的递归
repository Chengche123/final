状态转移方程:
    1 将原问题分解为子问题(可直接跳过)
    2 写状态转移方程: 1)寻找变量定义状态->选择 或者 2)归纳
    3 看状态转移方程有没有最优子结构和子问题重叠性质

base case:
    自底向上解法的初始化细节和自顶向下解法的base case是一样的

带备忘的自顶向下解法:
    之后: 带备忘的自顶向下解法可以根据状态转移方程直接写出

自底向上解法:
    1 根据状态转移方程归纳迭代顺序
    2 根据归纳结果写出代码
    3 优化空间复杂度(参考背包问题)

经典问题:
    1 背包问题
    2 扔鸡蛋:
        1) 状态转移方程: f(K,N) = min( max(f(K-1,i-1),f(K,N-i))+1 ){1<=i<=N}
           base case: f(0,0...)=0; f(0...,0)=0; f(1,0...N) = N;