1 索引字段的长度:
    一: 索引选取的越长，占用的磁盘空间就越大，相同的数据页能放下的索引值就越少，搜索的效率也就会越低。

2 索引字符串可以使用的方式
    一: 直接创建完整索引，这样可能比较占用空间；
    二: 创建前缀索引，节省空间，但会增加查询扫描次数，并且不能使用覆盖索引；
    三: 倒序存储，再创建前缀索引，用于绕过字符串本身前缀的区分度不够的问题；
    四: 创建 hash 字段索引，查询性能稳定，有额外的存储和计算消耗，跟第三种方式一样，都不支持范围扫描。
    五: 在实际应用中，根据业务字段的特点选择使用哪种方式。

