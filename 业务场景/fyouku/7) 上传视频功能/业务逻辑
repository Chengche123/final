1 上传视频文件接口:
    1)用uid和file字段以表单的形式提交数据,http请求方式为post
    2)对用户ID+精确到毫秒的时间戳做MD5,得到文件名
    3)保存视频到本地static/video文件夹,返回播放url

2 保存用户上传视频信息接口:
    1)用播放url,标题,副标题,频道id,类型id,地区id,用户id做通配请求
    2)保存视频相关的标题,副标题,添加时间,频道id,类型id,地区id,剧集更新时间,用户id到video表中
      保存视频相关的标题,添加时间,第几集,视频id,播放url,评论数到video_episodes表中
    3)返回状态信息





