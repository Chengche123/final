1 是什么
    一: ⽤来做适配，将不兼容的接⼝转换为可兼容的接⼝

2 实现:
    一: 类适配器和对象适配器。类适配器使⽤继承关系来实现，对象适配器使⽤组合关系来实现。

3 应用场景
    一: 封装有缺陷的接⼝设计 如对命名丑陋,参数过多,性能低下的接口进行二次封装
    二: 统⼀多个类的接⼝设计 将所有系统的接⼝适配为统⼀的接⼝定义,以便复用
    三: 替换依赖的外部系统 把项⽬中依赖的⼀个外部系统替换为另⼀个外部系统的时候，利⽤适配器模式，可以减少对代码的改动
    四: 兼容⽼版本接⼝ 将要废弃接⼝的内部实现逻辑委托为新的接⼝实现,平滑过渡

