1 无函数体函数:func Sin(x float64) float //implemented in assembly language

2 捕获迭代变量的错误写法:
    var rmdirs []func()
    dirs := tempDirs()
    for i := 0; i < len(dirs); i++ {
        os.MkdirAll(dirs[i], 0755) // OK
        rmdirs = append(rmdirs, func() {
            os.RemoveAll(dirs[i]) // NOTE: incorrect!
        })
    }
    正确写法要加上 i := i

